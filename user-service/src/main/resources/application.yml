server:
  port: 8081
spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/yun6
    username: root
    password: xdcf3233
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
mybatis:
  type-aliases-package: com.itzixue.user.pojo
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:10086/eureka,http://127.0.0.1:10087/eureka
  instance:
    prefer-ip-address: true
    ip-address: 127.0.0.1
